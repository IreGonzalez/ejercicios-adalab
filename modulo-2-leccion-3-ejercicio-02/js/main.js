'use strict';

//declaramos en primer lugar las variables

let userName = 'Pepita';

//declaramos los condicionales

if (userName === 'Irene' || userName === 'Jenny') {
    console.log(`Bienvenida, ${userName}`);
} else {
    console.log('Lo siento pero el usuario que has introducido no está registrado')
}